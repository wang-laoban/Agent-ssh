# test-agent 设计理念

本文档阐述 `test-agent` 的整体架构思想、安全设计原则、防篡改日志系统的演进路径，以及关键设计决策的取舍依据。

---

## 1. 设计目标

`test-agent` 要解决的核心问题是：

> 如何让一个不可信或半可信的网络客户端，安全地在一台受控测试机上执行任意命令，同时保证执行过程可审计、可验证、可回滚？

为此，系统追求以下目标：

1. **最小权限**：Agent 本身不应持有过多权限，凭据不硬编码，访问受限于系统用户权限。
2. **默认安全**：开箱即用即具备合理的安全默认值，而非依赖用户手动加固。
3. **防御纵深**：从网络、进程、文件、日志多个层面提供独立的安全边界。
4. **可验证的可观测性**：日志不是“写出去就完”，而是可以被独立第三方验证完整性的证据。
5. **平台无关**：同一套代码通过 Go 标准库与 build tag 支持 Linux、Windows、macOS。

---

## 2. 架构思想

### 2.1 分层解耦

```text
Controller/Client
       │
       ▼
┌─────────────┐
│   Gateway   │  HTTP 路由、鉴权、请求追踪、panic 恢复
└──────┬──────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
  Auth   Executor ──▶ TaskManager
   │       │
   │       ▼
   │  TamperProofLogger
   │       │
   │   ┌───┴───┐
   │   │       │
   ▼   ▼       ▼
  Token  本地日志   MQ
```

- **Gateway**：只关心 HTTP 语义，不碰业务逻辑。
- **Auth**：只关心 Token 生命周期，不碰命令执行。
- **Executor**：只关心单条命令的安全执行，不碰 HTTP 或任务调度。
- **TaskManager**：只关心异步任务状态机，不碰命令细节。
- **Logger**：只关心结构化记录与完整性保护，不碰业务状态。

每一层都可以被单独测试、替换或审计。

### 2.2 失败隔离

- HTTP 层 panic 被 recover 中间件捕获，不会拖垮整个进程。
- 单个命令执行超时不会影响其他并发任务。
- MQ 发送失败不会影响本地日志写入；本地日志失败会返回错误，避免“静默丢日志”。
- 配置加载失败会立即退出，避免以错误配置运行。

### 2.3 无状态优先

Agent 的核心业务状态集中在内存中的任务 Map 和 Token 缓存：

- 任务状态不持久化到数据库，完成/超时/被杀后按保留策略清理，简化部署。
- Token 持久化到受权限保护的文件，但内存缓存提供高并发鉴权能力。
- 日志是唯一的长期状态，采用 append-only 文件 + MQ 复制，避免复杂数据库依赖。

---

## 3. 安全设计原则

### 3.1 凭据不落地于配置文件

传统方案把 API Token 写入 `config.yaml`，存在以下风险：

- 配置文件通常会被提交到版本控制或共享给多人。
- 配置文件权限通常较宽（0644），任意用户可读。

`test-agent` 的解决方案：

- Token 由 Agent 启动时通过 `crypto/rand` 生成，写入单独文件。
- Token 文件权限强制为 `0600`，目录权限 `0700`。
- 配置文件只保存 Token 文件路径，不保存 Token 本身。
- Token 每日自动轮换，旧 Token 即时失效。

### 3.2 授权与系统用户权限绑定

- 只有能 SSH 登录测试机且能读取 Token 文件的用户，才能拿到当前有效 Token。
- 这天然复用了操作系统的账号体系、SSH 认证、sudo 审计，无需额外 IAM 系统。

### 3.3 命令执行的最小破坏面

- **白名单**：如果配置了 `allowed_commands`，只有首段命令在白名单内才允许执行。
- **黑名单**：正则匹配高危命令（如 `rm -rf /`、`mkfs`、`dd if=/dev/zero` 等），命中即拒绝。
- **工作目录隔离**：所有命令默认在 `work_dir` 下执行，上传文件也限定在 upload 目录。
- **输出截断**：使用 `io.LimitedReader` 限制 stdout/stderr 大小，防止内存溢出。
- **进程组清理**：Unix 下设置 `Setpgid: true`，超时或取消时 kill 整个进程树，避免孤儿进程。

### 3.4 上传安全

- 文件名经过清洗，去掉路径分隔符和危险字符。
- 保存前校验最终路径是否落在 upload 目录内，防止路径穿越。
- 原子写入：先写 `.tmp` 临时文件，再 `rename`，避免半写文件。
- 文件已存在时返回 409，防止意外覆盖。

### 3.5 日志脱敏

- 任何日志中都不打印完整 Token。
- 只打印 Token 的 SHA256 前 8 位前缀，用于追踪而不泄露凭据。

---

## 4. 防篡改日志设计

日志是系统安全的最后一道记录防线，但日志文件本身却常成为攻击者的首要清除目标。传统基于文件权限的保护（如 `chattr +a`）无法抵御 root 权限下的恶意操作，因此需要从架构层面设计一套 **篡改可发现、伪造可识别、灭迹不可行** 的日志保障体系。

### 4.1 方案演进

#### 第一层：文件级附加写保护（chattr +a）

- 设置 append-only 属性，限制已有内容的修改与删除。
- 实现简单，对普通误操作有效。
- **局限**：root 用户可随时撤销该属性，无法对抗高权限攻击。
- **结论**：仅作为基础辅助手段，不构成完整安全闭环。

#### 第二层：哈希链与外部锚定

- 每条日志包含前一条日志的哈希值，形成链式校验结构。
- 同时将链根哈希锚定至区块链等不可篡改外部账本。
- 篡改行为可被检测，具备较强的存证能力。
- **局限**：链式计算存在额外开销，不适合高频日志场景，属于“事后检测”而非“事前防护”。

#### 第三层（推荐架构）：MQ 解耦 + 多副本 + 签名序列号

```text
应用服务（Producer）
  ├─ 生成日志
  ├─ Ed25519 签名（log + timestamp）
  ├─ 分配唯一序列号
  └─ 异步发送至 MQ（Kafka/RocketMQ/Redis）

消息队列（高可用、至少一次投递）

消费者集群（独立部署）
  ├─ 消费者 A → 本地磁盘
  ├─ 消费者 B → 对象存储（OSS/S3）
  ├─ 消费者 C → 审计数据仓库
  └─ 定时任务：多副本哈希交叉校验 + 告警
```

`test-agent` 实现了 Producer 侧的全部能力：签名、序列号、哈希链、MQ 发送。Consumer 侧可由独立的审计系统实现，本仓库提供验证工具（`--verify-log`）作为 Consumer 侧的轻量示例。

### 4.2 四重保障机制

#### 1. 解耦与隔离

应用服务仅负责生成并发送日志，不持有最终存储权限。即便应用服务器被完全控制，攻击者也无法影响已经落盘的历史日志，因为存储由独立消费者管理。

#### 2. 多副本冗余与交叉校验

同一日志由多个消费者并行写入不同存储介质：

- 本地磁盘
- 云对象存储
- 审计数据库

攻击者若想彻底删除日志，必须同时攻破所有独立存储节点，攻击成本呈指数级上升。系统定期比对各副本全局哈希，发现差异即触发告警，实现篡改实时感知。

#### 3. 数字签名防伪造与防篡改

每条日志由 Producer 使用 Ed25519 私钥签名，Consumer 验签通过后才允许存储。

安全效果：

- 攻击者无法伪造合法日志。
- 修改日志内容会导致签名校验失败。

#### 4. 序列号防重放

每条日志携带全局唯一序列号。Consumer 根据序列号进行去重。即使攻击者截获合法日志并重复发送，也只能被接收一次，后续重复消息被拒绝，从而杜绝重放攻击。

### 4.3 日志条目结构

```json
{
  "seq": 42,
  "ts": "2026-07-30T00:51:09.3971793Z",
  "level": "INFO",
  "msg": "agent_started",
  "attrs": { "listen_addr": "127.0.0.1:18080" },
  "prev_hash": "886c47e6962e3ee7df0b24767bea7f5d17d3074756ac4c91ab2e15b2c7ea8675",
  "algo": "ed25519",
  "signature": "4388ad9f6360b8a23f58acc17bb98d032e858f7dec65a8a2ed46aff7f72e6a28..."
}
```

- `seq`：全局递增序列号，保证顺序与去重。
- `prev_hash`：前一条日志的 SHA256 哈希，形成链。
- `signature`：对 `seq + ts + level + msg + attrs + prev_hash + algo` 的 Ed25519 签名。

### 4.4 验证流程

1. 读取公钥。
2. 按顺序读取每条日志。
3. 检查 `seq` 是否连续。
4. 用公钥验证签名。
5. 检查 `prev_hash` 是否等于前一条日志的哈希。
6. 任一步骤失败即判定日志被篡改。

### 4.5 实施路径建议

- **阶段一：快速建立可验证能力**
  - 启用 `log.enabled=true`，生成签名日志。
  - 使用 `--verify-log` 建立完整性验证能力。

- **阶段二：核心架构升级**
  - 部署至少两个独立消费者。
  - 一个写本地磁盘，一个写对象存储或审计数据库。
  - 配置 `mq.type=redis` 或 `mq.type=kafka`。

- **阶段三：完备安全加固**
  - 增加定时交叉校验任务。
  - 增加篡改告警策略。
  - 将公钥分发到独立审计节点，形成完整闭环。

---

## 5. 关键设计决策

### 5.1 为什么用动态 Token 而不是 JWT/API Key？

- JWT 需要密钥管理，泄露后难以撤销。
- 静态 API Key 一旦泄露，影响周期长。
- 动态 Token 与文件系统权限绑定，天然支持每日轮换和即时撤销（通过 `POST /auth/rotate` 或文件删除）。

### 5.2 为什么用 slog.Handler 而不是独立日志库？

- `log/slog` 是 Go 标准库，无需额外依赖。
- 自定义 `slog.Handler` 可以无侵入地拦截所有日志调用，自动添加签名和哈希链。
- 原有代码中的 `logger.Info/Error/Warn` 调用无需修改即可享有防篡改能力。

### 5.3 为什么默认用 Ed25519 而不是 HMAC-SHA256？

- 设计文档推荐 HMAC-SHA256，但 HMAC 是 symmetric，需要安全分发共享密钥。
- Ed25519 是非对称签名，私钥留在 Agent，公钥可公开发给审计方，天然支持“生成端签名、消费端验签”的解耦架构。
- 同时保留 `hmac-sha256` 作为可选算法，满足需要对称密钥的场景。

### 5.4 为什么 MQ 默认实现是 file？

- 为了让项目在没有外部依赖的情况下也能运行和测试。
- `file` 模式将每条日志以独立文件写入本地目录，模拟“本地队列”。
- 生产环境可无缝切换到 `redis` 或 `kafka`，只需改配置，无需改代码。

### 5.5 为什么任务状态不持久化？

- 任务执行是 transient 的，完成后保留一段时间即可清理。
- 不引入数据库，降低部署复杂度。
- 如果需要持久化，可由 Consumer 从 MQ 消费任务事件并写入数据库。

---

## 6. 扩展方向

- **Consumer 实现**：提供独立的日志消费与校验服务，支持 OSS/S3/ClickHouse 等后端。
- **TLS/mTLS**：Gateway 层支持 HTTPS，Agent 与消费者之间支持 mTLS。
- **审计告警**：基于 MQ 消费者的交叉校验结果，接入 Prometheus/钉钉/企业微信告警。
- **命令审计增强**：记录命令完整内容哈希、执行结果哈希，与日志签名共同形成完整证据链。
- **Web UI**：提供任务状态、日志校验结果的可视化界面。

---

## 7. 总结

`test-agent` 的设计哲学可以概括为：

> **让每一次远程执行都可控、可审计、可验证。**

真正的安全不是“锁住文件”，而是通过：

- 架构解耦
- 冗余存储
- 密码学验证
- 持续完整性校验

构建复合型安全体系，使每一次删除都留下痕迹，每一份伪造都无法通过验证。
